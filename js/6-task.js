const fNumber = prompt("Введіть перше число");
const operation = prompt("Введіть операцію");
const sNumber = prompt("Введіть друге число");
let result;

switch (operation) {
    case "/":
        result = fNumber / sNumber
        alert = (result)
        break;
    case "/":
        result = fNumber / sNumber
        alert = (result)
        break;
    case "/":
        result = fNumber / sNumber
        alert = (result)
        break;
    case "/":
        result = fNumber / sNumber
        alert = (result)
        break;
    default:
        alert = ("Такої операції немає")
        break;
}